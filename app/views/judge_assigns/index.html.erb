<br>  <%= link_to 'Back', fairs_path %>
<br>
<h1>Scorer Assignments</h1>

<table>
  <thead>
    <tr>
      <th>Scorer</th>
      <th>Entry</th>
      <th>Assigned</th>
      <th>Shadow</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @judge_assigns.each do |judge_assign| %>
      <tr>
        <td><%= judge_assign.user.email_or_name %></td>
        <td><%= judge_assign.entry.entry_name %></td>
        <td><%= judge_assign.assigned %></td>
        <td><%= judge_assign.shadow %></td>
        <td><%= link_to 'Show', fair_judge_assign_path(@fair, judge_assign) %></td>
        <td><%= link_to 'Edit', edit_fair_judge_assign_path(@fair, judge_assign) %></td>
        <td><%= button_to 'Destroy', [@fair, judge_assign], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Scorer Assignment', new_fair_judge_assign_path(@fair) %>
<br>  <%= link_to 'Back', fairs_path %>
