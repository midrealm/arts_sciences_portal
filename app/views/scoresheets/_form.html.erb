<%= form_with(model: scoresheet, local: true) do |form| %>
  <% if scoresheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scoresheet.errors.count, "error") %> prohibited this scoresheet from being saved:</h2>

      <ul>
        <% scoresheet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :score_id %>
    <%= form.text_field :score_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :entry_id %>
    <%= form.text_field :entry_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
