<%= form_with(model: user_peerage, local: true) do |form| %>
  <% if user_peerage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_peerage.errors.count, "error") %> prohibited this user_peerage from being saved:</h2>

      <ul>
        <% user_peerage.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :peerage_id %>
    <%= form.text_field :peerage_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
