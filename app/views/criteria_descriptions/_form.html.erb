<%= form_with(model: criteria_description, local: true) do |form| %>
  <% if criteria_description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(criteria_description.errors.count, "error") %> prohibited this criteria_description from being saved:</h2>

      <ul>
        <% criteria_description.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :criteria_type %>
    <%= form.select :criteria_type_id, CriteriaType.all.map{ |m| [m.description, m.id] }%>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.select :category_id, Category.all.map{ |m| [m.name, m.id] }%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
